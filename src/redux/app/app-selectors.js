const getConsultDate = (state) => state.app.date;
const getConsultTime = (state) => state.app.time;
const getConsultFullDate = (state) => state.app.fullDate;


export default {
  getConsultDate,
  getConsultTime,
  getConsultFullDate 
};
